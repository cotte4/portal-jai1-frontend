/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface AlarmsControllerGetDashboard$Params {
  hideCompleted: string;
  level: string;
  cursor: string;
  limit: string;
}

export function alarmsControllerGetDashboard(http: HttpClient, rootUrl: string, params: AlarmsControllerGetDashboard$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, alarmsControllerGetDashboard.PATH, 'get');
  if (params) {
    rb.query('hideCompleted', params.hideCompleted, {});
    rb.query('level', params.level, {});
    rb.query('cursor', params.cursor, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

alarmsControllerGetDashboard.PATH = '/v1/admin/alarms/dashboard';
